@model List<ClassType>

<div class="get-div">
    <h1 style="font-size: 280%; text-shadow: 0 0 20px #FEFEFE;">Class Type</h1>

    <a class="back-btn" href="/Home/Index">Back</a>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var classType in @Model)
            {
                <tr>
                    <td>@classType.Id</td>
                    <td>@classType.Name</td>
                    <td>@classType.Description</td>
                </tr>
            }
        </tbody>
    </table>
</div>